# API Keys (no need if runs locally / publicly)
PLANNER_KEY=your-planner-api-key
PROVER_KEY=your-prover-api-key

# Planner Model Configuration
PLANNER_MODEL=gemini-2.5-flash
PLANNER_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/

# Prover Model Configuration
PROVER_MODEL=deepseek-ai/DeepSeek-Prover-V2-7B
PROVER_BASE_URL=http://127.0.0.1:8001/api

# Lean Verification Backend
VERIFICATION_URL=http://127.0.0.1:8000/api
VERIFICATION_KEY=your-verification-key

# Proxy Configuration (optional)
# Set HTTPS_PROXY or HTTP_PROXY to route requests through a proxy
# Example: HTTPS_PROXY=http://user:password@proxy.example.com:8080
#HTTPS_PROXY=

# SSL Verification (optional)
# Set to 'false' to disable SSL certificate verification (similar to curl --ssl-revoke-best-effort)
# Use with caution - only disable when behind corporate proxies that intercept HTTPS
#SSL_VERIFY=true
